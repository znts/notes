cron任务概述
  用途：按照设置的时间间隔为用户反复执行某一项固定的系统任务
  所需软件包：cronie crontabs
  系统服务名：crond
  日志文件：/var/log/crond
crontab任务记录
  分 时 日 月 周 命令（绝对路径）
  * ：匹配范围内任意时间
  ， ：分隔多个布连续的时间点
  - : 指定连续时间范围
  /n ：指定时间频率，每n...
 使用 crontab 命令
  -编辑：crontab -e [ -u 用户名]
  -查看：crontab -l [ -u 用户名]
  -清除：crontab -r [ -u 用户名]
